# handoff

Date: 2026-05-06
Session: post-review cleanup + code review process

## Pick up

### Ingest input validation (review issue #3)

The `/ingest` handler accepts arbitrary `profile` and `source` without running them through `validate::profile` or `validate::tags`. The MCP tools validate these — the HTTP endpoint should too. Bounded scope: add validation before enqueueing, reject with 400.

### Code review as a skill

`/home/josh/soft/code-review-prompt.md` was rewritten this session. Josh mentioned turning it into a Claude Code skill. Decide where it lives (user skill? agentops skill?) and wire it up.

### Integration test for /ingest

chitta/7 acceptance criteria includes "Integration test: ingest text via endpoint, verify memories appear in search." Not yet written.

### chitta/4 — search_memories updates (unblocked)

Add search filtering by external_refs. Check yojana task for original scope.

### Derivations tool wiring

DB functions exist (`insert_derivation`, `get_derivations_for`, `get_derived_from`). Wire into /reflect when that skill is ported to two-layer (manas-harness/15). Note: derivation queries don't filter soft-deleted rows yet — add `WHERE invalidated_at IS NULL` joins when wiring.

### Docs update

external_refs, soft-delete semantics, /ingest, and the new param structs are not reflected in README.md or docs/official/*.

## Context

### What just landed (commits e411148, 42a3469)

- Migration 0011: partial unique index on (profile, idempotency_key) WHERE invalidated_at IS NULL — fixes soft-delete + re-store bug
- Param structs: MemoryPatch, SearchParams, QueryLogInput, HybridSearchParams — zero clippy warnings
- Removed dead `max_importance` field from ingest
- Renamed `split_sentences` -> `split_chunks`
- Regression test: `restore_after_soft_delete_creates_new_row`

### Chitta is at zero clippy warnings, 97 tests passing

All integration tests exercise live Postgres + ONNX. Requires `TEST_DATABASE_URL` set.

## Blockers

- Astrobench hybrid eval blocked on bge-m3 re-export with sparse_weights.
