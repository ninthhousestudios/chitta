# handoff

## Pick up

### BGE-M3 model re-export (do first)

The upstream BAAI/bge-m3 ONNX model only has `sentence_embedding` and `token_embeddings` outputs — no `sparse_weights`. All astrobench eval configs produce identical results because sparse is a no-op. Need to re-export with sparse_weights output for meaningful hybrid retrieval comparison.

Prior context: the custom export at `prometheus-en-croute/bge-dense-sparse` stripped the colbert head and added `sparse_weights`, but the HF upload was truncated (~4MB short on onnx_data). Steps:
1. Re-export bge-m3 with colbert head stripped, sparse_weights output intact
2. Re-upload to prometheus-en-croute/bge-dense-sparse with complete files
3. Update `~/.chitta/models/bge-m3-onnx/` with the new model
4. Re-run astrobench sweep — sparse/FTS legs should now differentiate

### Astrobench — consider Qwen v3.6

After the bge-m3 re-export, evaluate whether to add a Qwen v3.6 embedder config to the benchmark. Would need:
- ONNX export of Qwen embedding model
- New `Embedder` implementation or adapter
- Possibly re-ingest with different embeddings (separate DB or new embedding column)

### Astrobench baseline results (for reference)

80 queries, 4 slices. Dense-only (all configs identical due to missing sparse):

| Slice | n | r@5 | r@10 | hit | MRR |
|-------|---|-----|------|-----|-----|
| A (jargon) | 30 | 90% | 100% | 100% | 0.68 |
| B (hapax) | 20 | 80% | 90% | 95% | 0.72 |
| C (plain) | 15 | 80% | 80% | 87% | 0.60 |
| D (multi-hop) | 15 | 63% | 80% | 100% | 0.56 |
| **Total** | **80** | **81%** | **90%** | **96%** | **0.65** |

Slice C (plain language) is weakest — 2 queries never find gold. Worth investigating which queries fail.

Query files: `bench/datasets/astrobench/queries/slice-{a,b,c,d}.jsonl`
Codex raw: `docs/queries/codex-slice-{a,b,c,d}.jsonl` (not used by eval)
Results: `bench/astrobench/outputs/`

## Non-astrobench items

### Architecture deepening

Findings in `docs/chitta-overall-refactor.md` — 6 ranked candidates ready for implementation planning.

- Candidates 1 (newtypes), 4 (dependency inversion), and 5 (curate lib.rs) align with aion-share engine/server split.
- Candidate 2 (retrieval consolidation) is highest-leverage for retrieval quality.
- Candidate 3 (extract admin) is low-risk, independent.
- Recency scoring duplication (db.rs + retrieval.rs) is a latent bug risk.

### aion-share refactor

1. **Engine/server crate split** (step A in `docs/plans/aion-share-refactor.md`)
2. **Principles-doc revision** for memory_type allowlist policy (step B prep)
3. **memory_type -> allowlist from config** (step B)
4. **Embedder sidecar extraction** (step C, after v0.0.3)

### Chitta delete guard

`delete_memory` does a hard delete — risky with subagent access. Plan: soft-delete with `invalidated_at` timestamp. Interim: PreToolUse hook gating on `$CLAUDE_CODE_AGENT_DEPTH`.

### Pre-existing

- **Smriti implementation.** Design at `docs/plans/smriti-sketch.md`. Repo at `~/soft/smriti`.
- **Mental-model retirement protocol** (phase 0.1). Not urgent.
- **Deploy document_ref removal.** Commit `a4c3722`. Minor doc drift in `src/mcp.rs` StoreArgs.

### Model re-export investigation

See "BGE-M3 model re-export" above — now the top priority.

### v0.1.0 config standardization (landed)

Binary renamed `chitta-rs` -> `chitta`. Config loads from `~/.chitta/.env`. Systemd service updated. Version 0.1.0 deployed and stable.

## Blockers

- Astrobench hybrid eval blocked on bge-m3 re-export with sparse_weights.

## Context

- **Astrobench plan** at `docs/plans/astrology-benchmark-plan.md`, query guide at `docs/astrobench-query-authoring.md`.
- **Decision records:** standalone eval architecture `019dd12c-27a3`, source-path gold IDs `019dfc67-dfb3`, first sweep results `019dfc65-313a`.
