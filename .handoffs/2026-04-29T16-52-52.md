# Handoff

## Pick up

### Astrobench — query authoring (Josh's turn)

Implementation complete. DB ingested (2,147 chunks across astro/iching/cards). Scripts at `bench/astrobench/`, docs at `bench/astrobench/how-to-run.md`.

**What Josh needs to do:**

1. **Take a pg_dump snapshot** (optional but recommended before authoring):
   ```bash
   pg_dump -Fc chitta_astrobench > bench/datasets/astrobench/snapshot-20260427.dump
   ```

2. **Run find-unique-tokens.py** for slice B candidates:
   ```bash
   cd bench/astrobench && uv run python find-unique-tokens.py
   ```

3. **Author queries** per `docs/astrobench-query-authoring.md`:
   - Slice A: ~30 Sanskrit/jargon queries (hand-written)
   - Slice B: ~20 exact-identifier queries (from hapax output)
   - Slice C: ~15 natural-language queries (hand-written)
   - Slice D: ~15 multi-hop queries (hand-written, 2+ gold chunks each)

4. **Resolve gold chunk IDs** against the bench DB (psql or run chitta-rs pointed at chitta_astrobench).

5. **Place JSONL files** in `bench/datasets/astrobench/queries/slice-{a,b,c,d}.jsonl`.

6. **Run the eval sweep**:
   ```bash
   cd bench/astrobench && uv run python eval.py --sweep
   ```

7. **Interpret results** against the decision matrix in `docs/plans/astrology-benchmark-plan.md`.

### Known optimization for next session

`ingest.py` commits after every chunk (~0.7 chunks/sec on CPU). Batching commits every 50 chunks would speed reruns significantly. Not urgent — ingest is a one-time cost and the current run completed.

### Open chitta hygiene items

1. Write LifeBench rrf-sweep summary from existing JSONs — `bench/results/pre-v0003-tests/lifebench-rrf-sweep/`.
2. Grep chitta-rs for: what fields go into the embedded string, whether query side gets BGE-M3 task-prefix instruction, any absolute-similarity thresholds in `src/retrieval/`.

### josh's notes

Some agents get confused about what profile to use.

Gemini reads from chitta, and then later there is an error:
```
[Feedback Details for "MCP ERROR (chittars)"] StreamableHTTPError: Streamable HTTP
error: Failed to open SSE stream: Not Found
```

### aion-share refactor

1. **Engine/server crate split** (step A in `docs/plans/aion-share-refactor.md`). Mechanical, ~2 days. Can land before v0.0.3 ships.
2. **Principles-doc revision** for memory_type allowlist policy (step B prep).
3. **memory_type → allowlist from config** (step B).
4. **Embedder sidecar extraction** (step C). After v0.0.3 lands.

### Chitta delete guard

`delete_memory` does a hard delete — risky with subagent access. Plan: soft-delete with `invalidated_at` timestamp. Interim: PreToolUse hook gating on `$CLAUDE_CODE_AGENT_DEPTH`.

### Pre-existing

- **Smriti implementation.** Design at `docs/plans/smriti-sketch.md`. Repo at `~/soft/smriti`.
- **Sangha implementation.** Design at `docs/claude-presence-rewrite.md`. Repo at `~/soft/sangha` (not created yet).
- **Mental-model retirement protocol** (phase 0.1). Not urgent.
- **Deploy document_ref removal.** Commit `a4c3722`. Minor doc drift in `src/mcp.rs` StoreArgs.

### Model re-export investigation

Currently using upstream BAAI/bge-m3 ONNX model directly. It includes the colbert head, which produces `token_embeddings` output that chitta ignores — wasted compute and ~2.5GB RAM. The custom export at prometheus-en-croute/bge-dense-sparse stripped the colbert head and added a `sparse_weights` output, but the HF upload is truncated (~4MB short on onnx_data). Next steps:
1. Re-export bge-m3 with colbert head stripped, sparse_weights output intact
2. Re-upload to prometheus-en-croute/bge-dense-sparse with complete files
3. Compare memory footprint (upstream ~2.5GB vs stripped model)

### v0.1.0 config standardization (just landed)

Binary renamed `chitta-rs` → `chitta`. Config now loads from `~/.chitta/.env`. Systemd service updated. Embedding output name fallback added (`sentence_embedding` for upstream, `dense_embeddings` for custom). Version bumped to 0.1.0. Not yet committed.

## Blockers

- Astrobench eval blocked on Josh authoring queries.
- v0.0.3 hybrid-retrieval work in flight. Aion-share steps C/D wait for v0.0.3.

## Context

- **Astrobench plan** at `docs/plans/astrology-benchmark-plan.md`, query guide at `docs/astrobench-query-authoring.md`.
- **Aion architecture** at `~/nhs/soft/astrology/aion/doc/architecture-plan-overview.md`.
- **Decision records:** standalone eval architecture `019dd12c-27a3`, astrobench corpus stats `019dd12c-47e9`.
- v0.1.0 deployed and stable. Config standardized to ~/.chitta/ pattern.
