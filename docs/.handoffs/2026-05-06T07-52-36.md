# handoff

## Pick up

### chitta/4 — search_memories updates (now unblocked)

Blocked by chitta/1 + chitta/2, both now done. This task should add:
- Search filtering by external_refs (e.g. "find memories referencing this file")
- Possibly expose invalidated_at in search results for audit views
- Check yojana task description for original scope

### chitta/5 — chitta show CLI

Human-readable CLI for browsing memories. Still needs-triage in yojana.

### manas-harness/11 — sideband daemon (now unblocked)

Depends on chitta external_refs (0007). The sideband daemon needs to link file-move notifications to chitta memories via external_refs.

### BGE-M3 model re-export

Still the top astrobench blocker. Sparse weights missing from ONNX model means hybrid retrieval experiments produce identical results. See prior handoff for full context.

## Context

### What just landed

Migrations 0007-0009 (commit a553aed):
- `external_refs` JSONB column on memories — `[{"kind": "file", "ref": "..."}]`
- `invalidated_at` soft-delete — delete_memory sets timestamp, all reads filter
- `derivations` table — lineage tracking, DB CRUD ready, not yet wired into tools

### Known loose ends

- **No validation on external_refs shape.** Any JSON is accepted. Could add schema validation in validate.rs if agents start writing malformed refs.
- **Old indexes not dropped.** `memories_profile_record_time_idx` and `idx_memories_profile_type_record` still exist alongside the new partial indexes. The old ones cover soft-deleted rows too, which is wasted space. Safe to drop in a future migration.
- **derivations not wired into tools.** DB functions `insert_derivation`, `get_derivations_for`, `get_derived_from` exist in db.rs. Wire into /reflect when that skill is ported to two-layer (manas-harness/15).

### Architecture deepening

Findings in `docs/chitta-overall-refactor.md` — 6 ranked candidates. None urgent but candidate 2 (retrieval consolidation) is highest leverage for any future retrieval work.

## Blockers

- Astrobench hybrid eval blocked on bge-m3 re-export with sparse_weights.
