# handoff

Date: 2026-05-06
Session: chitta v0.2.0 — auto-extraction pipeline + cleanup

## Pick up

### Verify hook input format

The PostToolUse hook (`~/.claude/hooks/chitta-post.sh`) reads `tool_name` and `tool_output` from stdin JSON. Confirm these are the actual field names Claude Code passes to PostToolUse hooks — if not, fix the jq selectors. Same for PreCompact hook reading `transcript`.

### max_importance cap

The ingest endpoint accepts `max_importance` but the extraction worker doesn't enforce it yet. The field flows through to `IngestItem` but `process_item` ignores it. Wire it into the store step when importance scoring is added.

### Integration test for /ingest

chitta/7 acceptance criteria includes "Integration test: ingest text via endpoint, verify memories appear in search." Not yet written.

### chitta/4 — search_memories updates (now unblocked)

Blocked by chitta/1 + chitta/2, both now done. This task should add:
- Search filtering by external_refs (e.g. "find memories referencing this file")
- Possibly expose invalidated_at in search results for audit views
- Check yojana task description for original scope

### chitta/5 — chitta show CLI

Human-readable CLI for browsing memories. Still needs-triage in yojana.

### BGE-M3 model re-export

Still the top astrobench blocker. Sparse weights missing from ONNX model means hybrid retrieval experiments produce identical results. See prior handoff for full context.

## Context

### What just landed (v0.2.0, commit 4f56637)

- POST `/ingest` HTTP endpoint — 202 Accepted, queues to mpsc(256) channel, bearer auth required
- Background extraction worker — sentence split, BGE-M3 anchor classification (6 anchors, cosine threshold 0.45), narration filter, idempotent store via hash(chunk|project|source)
- external_refs shape validation — closed set of 6 kinds (file, commit, yojana_task, memory, url, session), wired into store_memory and update_memory
- Migration 0010 — dropped old pre-soft-delete indexes (`memories_profile_record_time_idx`, `idx_memories_profile_type_record`)
- PostToolUse and PreCompact hooks at `~/.claude/hooks/`, wired globally in settings.json

### Known loose ends

- **derivations not wired into tools.** DB functions `insert_derivation`, `get_derivations_for`, `get_derived_from` exist in db.rs. Wire into /reflect when that skill is ported to two-layer (manas-harness/15).
- **Anchor threshold tuning.** 0.45 cosine similarity was chosen as a starting point. Monitor extraction logs (`journalctl --user -u chitta-rs`) to see if it's too aggressive or too permissive.

### Architecture deepening

Findings in `docs/chitta-overall-refactor.md` — 6 ranked candidates. None urgent but candidate 2 (retrieval consolidation) is highest leverage for any future retrieval work.

## Blockers

- Astrobench hybrid eval blocked on bge-m3 re-export with sparse_weights.
